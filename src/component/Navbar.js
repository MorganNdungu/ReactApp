const Navbar = ()=>{
    return(
    <nav className="Nnavbar">
        <h2>TodoApp...</h2>
        <div className="links">
            <a href="#">Home</a>
            <a href="#">Add New</a>
        </div>
    </nav>
    )
}

export default Navbar;